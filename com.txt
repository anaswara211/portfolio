- pseudo class: used to define a special state of an element.
    refer-W3Schools
    -syntax:: slector:pseudo-class{ property:value; .....}
    mouse over-hover
    to remove inline -use inline-block for button - it will shift button bit down
- Grid : used to display content as row and column, user can set width of row as well as column
        refer W3Schools
    - grid container: display as grid, 
        - grid template column/row : used to set width of column/row
    - grid-item:
    ur : fr(frame unit(fractional unit))
    -linear gradient : refer W3Schools
        -direction
        -color combination
    - animations - using key frames (0% from==to 100%)duration 
   -different symbols in html &copy    &reg
   animation website -animate.css - copy link tag in head tag - apply only on block elements not in inline
   aos animate(link:https://michalsnik.github.io/aos/)- same
   js cdn link is placed after body tag
   *locomotive scroll -(advanced)
   -Webhosting : Webserver(free)
    - Netlify
    - github pages
    - Vercel
                    GITHUB
    - GIT: version controlling system
    - GIT commands
        - git status : used to check the status of git inside the folder
        - git init : used to create/initilize git repository
        - git add . : used to track all the untracked file in the folder to git repository.[here . means all files, add means track]
        - git commit -m "meassage"  (*/ changes /*) : used to save 